/**
 * MOVED. The two brand palettes live in `@cartze/core` now.
 *
 * See `tokens.ts` beside this for why the seam exists rather than a hundred
 * rewritten imports in one commit.
 */
export * from "@cartze/core/theme/themes";
