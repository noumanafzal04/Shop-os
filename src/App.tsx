/**
 * STUB — structure only. Nothing in this folder runs yet.
 *
 * The root. In order: safe-area provider, theme provider, react-query
 * client, toast host, then the navigator.
 *
 * Copied in shape from `mobile/src/App.tsx` on purpose — Phase 0 lifts the
 * providers into the shared package and both apps then mount the same ones.
 *
 * ── One thing this app does NOT have ─────────────────────────────────
 *
 * A mode switch. The customer app swaps its whole navigator between
 * shopping and riding; a shopkeeper has one hat. `ModeSwitchCover`,
 * `modeStore` and the two palettes' switching logic do not come across.
 */
