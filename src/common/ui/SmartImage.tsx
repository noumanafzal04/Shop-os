/**
 * MOVED to `@cartze/core`. This line is the seam — see `src/theme/index.ts`
 * for why the app keeps forwarding files rather than repointing every
 * import in the commit that moves them.
 */
export * from "@cartze/core/ui/SmartImage";
