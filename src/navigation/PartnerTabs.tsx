/**
 * STUB — structure only.
 *
 * Five tabs: Dashboard · Orders · Menu · Money · Account.
 *
 * Shop settings hang off Account rather than taking a sixth tab — see
 * `tabsFor.ts`. The one setting that cannot wait, open/closed, is on the
 * Dashboard.
 *
 * Orders carries a badge — the count of orders still waiting to be
 * accepted. It is the only badge in the app, because a badge that appears
 * on three tabs tells you nothing about where to look.
 */
