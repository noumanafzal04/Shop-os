/**
 * STUB — structure only.
 *
 * The landing tab: what needs doing right now, and nothing else.
 *
 *   GET /dashboard        today's takings, order counts
 *   GET /orders?stage=…   the ones still waiting to be accepted
 *
 * ── Read-only by design ──────────────────────────────────────────────
 *
 * Every number here is a link into the tab that owns it. A figure that can
 * be acted on in two places is a figure that gets acted on twice.
 *
 * The one control it carries is the shop's own switch: OPEN / CLOSED. It
 * is the single most urgent thing a shopkeeper reaches for — the kitchen
 * is behind, close for twenty minutes — and burying it three taps into
 * Settings is how orders get accepted that cannot be made.
 */
