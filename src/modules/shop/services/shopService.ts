/**
 * STUB — structure only.
 *
 * shop read/update, logo, banner, hours.
 *
 * `PUT /shop` takes the settings JSON. Send a MERGE of what is there, not
 * a fresh object: `forceFill(['settings' => …])` replaces the whole column
 * and has already wiped a delivery radius in this codebase. The endpoint
 * merges; a client that builds the object from its own form fields does
 * not.
 */
