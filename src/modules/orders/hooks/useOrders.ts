/**
 * STUB — structure only.
 *
 * react-query wrappers, and the invalidation rules.
 *
 * Advancing an order invalidates the queue AND the per-stage counts — they
 * are one fact and a stale badge over a fresh list is the bug that makes a
 * shopkeeper stop trusting the number.
 */
