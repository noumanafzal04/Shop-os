/**
 * STUB — structure only.
 *
 * The stage strip, with each stage's own count from the server.
 *
 * One vocabulary across the whole product: the rider app, the panel queue
 * and this bar all name stages identically. `OrderStatus` has never been
 * renamed and must not be paraphrased here — "Cooking" is not a stage.
 */
