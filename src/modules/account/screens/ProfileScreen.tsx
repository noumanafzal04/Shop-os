/**
 * STUB — structure only.
 *
 *   PUT  /auth/profile
 *   POST /auth/password/change
 *
 * Changing an email or phone drops its verified mark, deliberately — the
 * server does that and the screen has to say so before it is pressed.
 */
