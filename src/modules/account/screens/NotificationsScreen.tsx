/**
 * STUB — structure only.
 *
 *   GET  /notifications
 *   POST /notifications/{id}/read
 *   POST /notifications/read-all
 *
 * The in-app feed, which works TODAY and does not wait on Firebase. Until
 * push exists this is the only way a shop learns about anything that
 * happened while the app was shut — so it ships in Phase 1, not Phase 5.
 */
