/**
 * STUB — structure only.
 *
 * react-query wrappers.
 *
 * Recording an expense invalidates the dashboard as well as the expense
 * list — `profit` moves the moment an expense is saved, and a tile still
 * showing the old figure is the reason nobody trusts the tile.
 */
