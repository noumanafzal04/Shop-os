/**
 * STUB — structure only.
 *
 *   summary      GET  /reports/summary
 *   sales        GET  /sales
 *   sale         GET  /sales/{id}
 *   commission   GET  /commission
 *   margins      GET  /reports/margins      (what actually makes money)
 *   expenses     GET  /expenses
 *   addExpense   POST /expenses
 *
 * Every figure on these screens is the SERVER's. Nothing is summed on the
 * phone — a total computed in two places is a total that disagrees with
 * itself the first time either rounding rule changes, and the panel and
 * this app must never show a shopkeeper two different numbers for one day.
 */
