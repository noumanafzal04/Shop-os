/**
 * STUB — structure only.
 *
 *   GET/POST/PUT/DELETE /collections
 *
 * A collection is a shelf a shop arranges for itself — "Ramzan deals",
 * "New". Separate from categories, which are what a thing IS.
 */
