/**
 * STUB — structure only.
 *
 *   GET/POST/PUT/DELETE /categories
 *   POST /categories/reorder
 *
 * Order matters: this is the order a customer sees on the shop page, and
 * the order the chip bar uses. Reordering is the one thing here worth
 * doing on a phone.
 */
