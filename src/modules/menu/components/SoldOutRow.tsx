/**
 * STUB — structure only.
 *
 * A product row with its switch. The switch is the row's point, so it is
 * the largest target on it — not a chevron that opens a form that holds a
 * toggle.
 *
 * A product WITH sizes cannot answer with one switch: it opens a sheet
 * listing the sizes, each with its own. A parent toggle that silently
 * meant "all sizes" is how a shop turned off a large and lost the small.
 */
