---
name: shopos-mobile-design
description: ShopOS mobile app design direction (user-approved) + current build state
metadata: 
  node_type: memory
  type: project
  originSessionId: a4ba9d48-2a02-4ea8-81fb-a04eeaffd6b9
---

**Design direction (user, 2026-07-14; FINAL palette from a swatch screenshot):** **#3BB77E green · #010F1C ink · #999999 muted gray · #EDE9E0 cream** (theme.ts brand scale built around #3bb77e). **FLAT — no shadows** (user explicit, twice): cards separate via 1px borders + bg contrast; only the cart FAB keeps a whisper (shadow.lg). Neat/clean, perfect spacing+typography, market-level. **Footer (user-approved from foodpanda ref): Food · Grocery · [center green Cart FAB, raised, white ring, count badge] · Orders · Account** — FoodTab = CustomerHomeScreen (the full home), GroceryTab = MarketScreen initialParams {business_type:'grocery'}, Favorites demoted to a stack route (Account menu). Home gained a foodpanda-style **quick-actions tile row** (Offers / Pick-up / New shops / Top rated → ShopList presets). ProductSheet restyled to foodpanda sheet: full-width 190px hero image, floating ✕, name left + green price right, group headers w/ "select upto N" + Required/Optional pill, option rows w/ price then RIGHT-side checkbox (green ✓) / radio, "View N more" collapse (>4 options), footer qty stepper + big "Add to cart · total".

**Build state (Step 1-2+ done, 2026-07-14):** repo `shopos-mobile` (RN CLI + TS, react-navigation, react-query, zustand, keychain, lucide). Pre-existing scaffold had BOTH shop-owner area and customer area — kept; customer side rebuilt:
- `src/theme.ts` — single source; brand scale swapped to green, ink/cream added, shadow.sm/md = {} (flat).
- `src/stores/locationStore.ts` — GPS→`/marketplace/locate`→city (NO city picker); states idle/locating/located/denied/unserved; `setPin()` for manual pin; Android runtime permission; @react-native-community/geolocation (jest-mocked in jest.setup.js).
- `CustomerHomeScreen` (Home tab): dark ink header (greeting+bell, location row w/ detect, white pill search→Search screen), business-type emoji chips→ShopList, banner carousel (bannerClick+navigate), Near you (horizontal, distance+rating+Closed), Top rated. Pull-to-refresh, skeletons, unserved notice.
- `SearchScreen`: universal search (useUniversalSearch, debounced 250ms, min 2 chars) — grouped Products (price+strike+shop+distance) / Shops / Categories.
- `CartScreen` (FAB target): line list w/ qty steppers (trash at 1), pinned subtotal+Checkout Now→Checkout{slug}.
- `AccountScreen`: guest→sign-in prompt; profile card + menu (Addresses/Favorites-tab/Reservations/Notifications/Sign out via useLogout).
- `AddressesScreen` (customer_addresses CRUD, add uses current GPS pin, default handling), `NotificationsScreen` (GET /notifications).
- `marketplaceService` + hooks gained locate/home/search/bannerClick + geo params; PublicShop gained is_open_now/distance_km/delivers_to_me etc.
- Navigation: CustomerTabParamList = HomeTab/FavoritesTab/CartTab/OrdersTab/AccountTab; stack += Search/ShopList(=MarketScreen w/ route params business_type+title+geo)/Reservations/Notifications/Addresses. Account→Favorites must navigate("Tabs",{screen:"FavoritesTab"}).
- Verified: tsc clean, 7 jest tests green. NOT yet run on device/simulator; geolocation needs pod install/gradle link at build time.

**Step 4-5 done (2026-07-14):**
- `cartStore` upgraded: CartLine carries sold_by/unit_label/modifier_option_ids/modifiers_label; **keyOf includes sorted modifier ids** (same item, different options = separate lines); add(shopSlug, line, qty); decimals round3; count() = distinct lines.
- `ProductSheet` (marketplace/components): bottom-sheet Modal configurator — image, sale strikethrough, variant radio (out-of-stock disabled), modifier groups w/ min/max enforcement + is_default preselect, unit/weight qty stepper (0.25 steps, unit suffix), live `Add · total`, invalid = disabled.
- `MarketShopScreen` rebuilt: round icon top bar (back/phone/heart-fill), header card w/ rating/distance/Open-Closed/out-of-range badges, product cards w/ image thumb + sale strike + /unit + "customizable" + not-available states; simple products quick-add (＋), configurable → sheet; services → Contact (login-gated phone alert); sticky pill cart bar.
- `CheckoutScreen` rebuilt: **saved addresses picker** (default preselected, "type a different address" fallback), sends latitude/longitude (saved-address pin ?? live GPS pin), coupon field (server-applied), notes, qty steppers w/ weight steps + modifier labels, closed-shop warning banner, delivers_to_me=false pin warning, **friendly error copy by ApiError.errorCode** (SHOP_CLOSED/INSUFFICIENT_STOCK custom; OUT_OF_DELIVERY_AREA/MIN_ORDER_QTY use server text), COD note. PlaceOrderPayload gained lat/lng/coupon_code/notes/modifier_option_ids.
- PublicProduct type: original_price/brand/unit/sold_by/min_order_qty/available_now/modifier_groups. useMarketShop(slug, {lat,lng}).
- Verified: tsc clean, 7 jest green.

**Step 6 done (2026-07-14) — orders + push + deep links:**
- `OrderTrackingScreen` (stack route `Order:{id}`, uses existing GET /customer/orders/{id}): vertical timeline (pending→confirmed→preparing→ready|out_for_delivery→completed, fulfillment-aware), green check dots + rail, cancelled state, delivery address card, items+totals w/ COD, cancel while pending/confirmed. `useMyOrder(id)` **polls every 20s while the order is open** (refetchInterval fn). OrdersScreen: tap → tracking.
- `src/navigation/deepLinks.ts`: `navigationRef` (loosely typed Record<string,object|undefined>) + `resolveDeepLink(link)` mapping backend data.link → screens (orders/{id}→Order, orders→OrdersTab, announcements→Notifications, reservations, shop/{slug}→MarketShop; strips shopos:// prefix; unknown = ignored) + **pending-link queue replayed via NavigationContainer onReady={flushPendingDeepLink}**. Unit tested (6 cases, __tests__/deepLinks.test.ts — jest.mock hoisting gotcha: factory must lazily deref `mockNavigate` via wrapper fn).
- `src/services/push.ts`: **Firebase OPTIONAL at build time** — dynamic `require("@react-native-firebase/messaging")` in try/catch; app runs w/o it. When present: requestPermission → getToken → POST /devices {token, platform}; onTokenRefresh re-registers; onNotificationOpenedApp + getInitialNotification → resolveDeepLink(msg.data.link). `teardownPush()` (unsubscribe + DELETE /devices) wired into useLogout onSettled; `initPush()` fired from RootNavigator effect when status==='authenticated'. Activation steps in `docs/FCM-SETUP.md` (google-services.json, gradle plugin, FCM_SERVER_KEY backend env).
- Verified: tsc clean, **13 jest tests green**.

**Foodpanda-UX pass (2026-07-14, user shared 15 foodpanda screenshots — "I need this type UI/UX, home lengthy, shops base, location change from top header"):**
- **Geoapify geocoding** (user-provided key <redacted — see .env, key rotated 2026-08-08> in src/common/config.ts as GEOAPIFY_API_KEY + MAPS_PROVIDER='geoapify'|'google' + empty GOOGLE_MAPS_API_KEY — Google swaps in later w/ zero caller changes). `src/services/geo.ts`: reverseGeocode(lat,lng)→street label + searchAddress(text,bias)→suggestions (autocomplete, filter countrycode:pk); both fail-soft (null/[]).
- **LocationScreen** (modal, header location row → navigate("Location")): "Use my current location" (brand-tinted row), Geoapify address autocomplete list, saved addresses (authed) w/ default tag → setPin(lat,lng,label). locationStore.setPin now **upgrades label to reverse-geocoded street** async (foodpanda "49 Saeed Street" style) with stale-pin guard.
- **Home lengthened**: added `deals` section to backend /marketplace/home (discounted products across visible shops, deepest %-cut first, percent_off + shop + distance_km; test asserts 25% off) → **"Deals for you" carousel** (168px cards, image, green "%off" pill, price + strikethrough, shop·distance) + **"Explore shops"** vertical list (nearby, wide cards) at page bottom. Home order: dark header (greeting/location/search) → quick tiles → type chips → banners → Near you → Deals → Top rated → Explore shops.
- Emulator: AVD **Pixel_8_Pro** exists; emulator networking = 10.0.2.2 (DEVICE_TEST_URL=null, backend APP_URL=http://10.0.2.2:8000 so image URLs load in-emulator, artisan serve --host=0.0.0.0). For real device: cloudflared quick tunnel (ngrok needs user authtoken ERR_NGROK_4018; trycloudflare API 500s sometimes — retry). Release APK signs w/ debug keystore (template default).

**EMULATOR-VERIFIED (2026-07-14, Pixel_8_Pro AVD, debug build + metro):** sign-in (green theme) → user1 login → Food home renders full design: ink header w/ "Hareer, Gulberg" (Geoapify reverse-geocode LIVE), quick tiles, type chips, banner carousel (seeded PNGs load via 10.0.2.2), Near you w/ distances, **Deals for you** (15% off badges + strikethrough + shop·km), Top rated (Readers Corner shows **Closed** badge — hours logic live), Explore shops tail; Grocery tab (nearest-first list + city chips); cart FAB badge; **LocationScreen live: typed "johar" → Geoapify suggestion "Gulistan e Johar, Karachi Division"**. Emulator gotchas: `adb shell input text` drops `@` (escape as `\@`); first bundle can ANR SystemUI (tap Wait); grant ACCESS_FINE_LOCATION + `adb emu geo fix 74.3587 31.5204` for GPS; `adb reverse tcp:8081` for metro.

**Polish pass (2026-07-14, user: "grocery looks not good, bottom space issue, top/footer notch not perfect, hero bg → green primary"):** ① Hero header now **brand green** (user rejected ink for the hero; ink stays for text/accents) — welcome text brand-100, bell rgba(255,255,255,.18), white location text. ② `FocusedStatusBar` component (renders only when focused — tab screens stay mounted so plain StatusBar leaks styles): light-content on green screens. ③ Tab bar via `useTabScreenOptions()` hook: height 56+insets.bottom, paddingBottom max(insets.bottom,8) — fixes footer gesture-pill collision (was fixed height 62). ④ **MarketScreen rewritten** (Grocery tab + all ShopList routes): green hero header (title+subtitle+white search pill, back button only when pushed — isTab checks route.name), "Deals near you" strip scoped by business_type (filters home-feed deals client-side), designed shop rows (60px logo tile, type·city, ⭐+km+green Open/red Closed, chevron). Emulator-verified: green home, grocery deals strip w/ Basmati Rice 15% off, Account menu, footer spacing correct.

**Shop detail page DONE (2026-07-14, emulator-verified):** MarketShopScreen rewritten foodpanda-style, ALL logic preserved (cart.add/ProductSheet/favorites/reserve/contact). Layout: 168px hero (gallery[0] else green block w/ giant initial) + floating back/phone/heart white circles → centered name + "⭐4 (3 ratings) · 📍km · Open now" → **Delivery|Pick-up segmented toggle** (delivery hidden when feature off) → info card (🚴 Delivery {prep}–{prep+20} min · Rs fee / 📍 delivers within X km / 🕐 Cash on delivery; pickup → ready in ~prep min) → warn strips (closed / **"outside delivery range — try Pick-up"** from delivers_to_me) → menu search pill → **category chips (All + shop categories → server category_id filter)** → product rows (image, cat, price+strike, /kg for weight, green + or Reserve/Contact) → floating green pill cart bar "View cart · Rs X →". Whole page scrolls (everything in FlatList ListHeaderComponent). Backend: publicShop now exposes `prep_time_minutes`; mobile products() accepts category_id. Verified live: toggle, chips filtering, weight pricing Rs 320/kg, sale strikethrough, range warning at 270 km.

**Cart + Checkout reference layout DONE (2026-07-14, emulator-verified):** shared `StepBar` component (①Menu ─ ②Cart ─ ③Checkout, green filled to active). CartScreen: step bar (2), shop name, line items w/ pill steppers (trash at last step, weight steps 0.25), "+ Add more items"→MarketShop, **"Popular with your order" cross-sell** (same-shop simple in-stock items not in cart, horiz cards w/ + add), pinned Total + "Confirm payment and address"→Checkout. CheckoutScreen (logic untouched): step bar (3), **Payment method row 💵 Cash on delivery + amount**, existing totals/coupon/notes/addresses kept; success → navigate("Order",{id}) live tracking (was OrdersTab). **RUNTIME-PROVEN: a real order (ORD-000002, Rs 6,440, pickup COD, correct decimal-qty line totals) was placed from the emulator through the new checkout into Laravel; cart auto-cleared; cancel flow also exercised.** Screenshot-verified checkout: step bar, 0.5 kg weight stepper, Pickup|Delivery(+Rs150) toggle, payment row, Place order · Rs 2,200.

**Emulator automation gotchas:** taps QUEUE while JS is still bundling and all fire once loaded (caused a blind order placement) — always wait for `logcat -s ReactNativeJS:I | grep 'Running "ShoposMobile'` + ~10s before scripted taps; Android back key exits to launcher from tab root. Tap coords (raw 1344x2992): tabs y≈2860 — Food 135 · Grocery 405 · FAB 672 · Orders 940 · Account 1210; shop hero back (112,235).

**Search tabs DONE (2026-07-15, emulator-verified) — REFERENCE SCREEN SET COMPLETE.** SearchScreen: result tabs **All | Products | Shops** (green underline), filter chips **Open now / 4★ & up** (client-side on shops), "N results for query" count, grouped PRODUCTS/SHOPS/CATEGORIES sections (uppercase micro-titles), category chips set the query on tap. Verified live: "pi" → 5 results, pizza products ranked above "PVC Pipe", shop + Pizzas(1) category chip. Also re-verified restaurant shop page (prep 25→"25–45 min", 8 km radius, menu chips, seeded favorite ♥ filled).
**ALL foodpanda-reference screens now built & emulator-verified:** Home (lengthy) · Location picker (Geoapify) · Grocery tab · Shop page (grocery + restaurant) · Product sheet · Cart (step bar + cross-sell) · Checkout (payment row, real order placed) · Order tracking · Search tabs · Account.

**Order Fulfillment Configuration (2026-07-15, user spec — per-business pickup/delivery/both):**
- Backend: ShopSettings keys `pickup_enabled` (default true), `delivery_enabled` (default true), `min_order_amount`, `free_delivery_threshold` (+existing delivery_radius_km/prep_time_minutes/delivery_fee). `Tenant::pickupEnabled()` / `deliveryEnabled()` (= features.delivery && setting). Settings save guard: both off → 422 FULFILLMENT_REQUIRED. OrderService: PICKUP_DISABLED / DELIVERY_DISABLED (now via deliveryEnabled) / **MIN_ORDER_AMOUNT** (delivery-only, subtotal < min) / **free-delivery waiver** (subtotal ≥ threshold → fee 0). publicShop: `fulfillment:{pickup,delivery}` + min_order_amount + free_delivery_threshold; delivers_to_me uses deliveryEnabled. Seeder: retail/books/hardware = pickup-only; restaurant min 500/free 3000; grocery min 300/free 2500. **FulfillmentConfigTest (6 tests) — 386 total green.** Live-verified: fashion-hub → {pickup:true, delivery:false}.
- Web portal: ShopSettingsPage "Order fulfillment" section — 🏬 Pickup / 🚚 Delivery toggles (inline error when both off), delivery sub-fields (radius/prep/min order/free-above) shown only when delivery on. OwnerOrdersPage already shows 🚚 Deliver-to / 🏬 Pickup per order (spec satisfied).
- Mobile: PublicShop type +fulfillment/min/free. MarketShopScreen: both → toggle; single mode → static "🚚 Delivery only"/"🏬 Pickup only" pill; effect snaps state to supported mode (NOTE: effect must sit AFTER `const shop = useMarketShop(...)` — TDZ); info card shows Min. order + Free-delivery-above lines. CheckoutScreen: chips gated per mode, snap effect, **client-side fee waiver estimate + "🎉 earned FREE delivery"**, below-minimum warning ("add Rs X more or switch to pickup") + Place-order disabled (server authoritative). tsc+jest green. (Emulator died at end of session — mobile UI verified by tsc/tests only for this slice; user said no live emulator check needed.)
- Phase 2 (user-listed, future): merchant riders, third-party delivery partners, live GPS tracking, driver app, delivery assignment.

**Still pending (mobile):** map-pin picker (Google Maps phase); OTP login; foreground push toast; icon/splash/signing + release build for physical device (needs fresh tunnel URL in config).
