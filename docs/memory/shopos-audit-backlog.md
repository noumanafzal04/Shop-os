---
name: shopos-audit-backlog
description: 2026-07-24 4-agent per-business-type audit — P0 khata ledger + 10 P1s found (fix session started same day); July P0s confirmed FIXED
metadata: 
  node_type: memory
  type: project
  originSessionId: a4ba9d48-2a02-4ea8-81fb-a04eeaffd6b9
  modified: 2026-08-06T14:44:46.079Z
---

**2026-07-24 second full audit** (4 parallel agents: Food / Mart+Retail / Pharmacy / Services+cross-cutting), superseding the 2026-07-21 audit. All July P0s (combo/pack cancel+return stock loss, trusted modifiers, tax authority, split tender, idempotency replay) CONFIRMED FIXED + regression-locked (P0RegressionTest, OnlineOrderParityTest).

**VERIFIED SOLID (don't re-audit):** server-authoritative pricing (no client unit_price/tax accepted); InventoryService single write-path (row-lock/negative-guard/idempotency/FEFO/expired-fence); modifier min/max via one ModifierResolver in POS+dine-in+online; plan limits live-counted, CSV import gated; marketplace leak defense at tenant scope (`marketplaceVisible` requires features.marketplace); dine-in mechanics (double-tab lock, double-settle guard, void exclusion, KOT price-free); reservation lifecycle (row-locked accept, idempotent release, trusted complete); pack base-unit math; legacy business codes all resolve.

**ALL P0/P1s BELOW FIXED 2026-07-24 SAME SESSION — 506 tests green (+17 in MarketLevelFixesTest). Implementation notes: khata reversal via `Customer::outstandingCreditForSale()` cap + ledger entries w/ sale_id; per-line `tax_rate` snapshot on sale_items + `refund_tax` on sale_returns (refund_total=base+tax); dine-in settle → trusted path w/ unit_price/line_total/modifiers snapshots (trusted now allows inactive products, honors line_total, computes tax when no 'tax' key); CSV stock via InventoryService 'set' (new_quantity!) + seen-set dup barcode/PLU guards + per-row QueryException catch + item_type-vs-business-type check; variant OPENING lots; RESTOCK lot on restore-without-live-lot (only if target has batches); Rx = RX_IN_PERSON_ONLY online (POS stays warn-only by design); MIN_ORDER_QTY at POS (non-trusted); services reservations default false; serving-window required_with pairing; combo_items deal-only + deal-variants prohibited (create+update); CatalogFoundationTest/BusinessTypeP1Test setUp tenants made business_type=null (capability tests). Findings list for reference:**
- **P0** khata: ProcessSaleReturnAction/CancelSaleAction never reverse `customer.credit_balance` (chargeCredit has only 1 counter-caller: manual repayment).
- **P1s:** (1) returns refund pre-tax only (sale_returns has no tax col); (2) dine-in SettleTicketAction settles NON-trusted → re-prices from live product; 86'ing/modifier-edit makes tab unsettleable (fix = pass snapshot + trusted_prices like OrderService::complete); (3) CSV re-import UPDATE path mass-assigns stock_quantity bypassing InventoryService; (4) variant medicine opening stock not batch-backed (CreateProductAction makes OPENING lot only product-level); (5) InventoryService restore refs top-up only an existing live lot — none found → stock/batch drift; (6) item_type validated only globally, never vs tenant business_type (CatalogFoundationTest even asserts cross-type creation works); (7) requires_prescription display-only — online order of Rx possible if pharmacy enables marketplace; (8) CSV import: no dup-barcode check (products.barcode has no DB unique), dup PLU = uncaught 500 aborts whole file; (9) min_order_qty online-only, not POS; (10) services default reservations=true but ReservationService filters type=Product → unusable for services.
- **P2 backlog:** half-set serving window silently 24/7; UpdateProductRequest lets duration/windows leak cross-type; combo_items accepted on non-deal (orphan rows); deal can carry variants; addons capability dead (no write path); service variants declared optional but prohibited; exchange downgrade refunds cash regardless of tender; return_number count()+1 race; PO receive doesn't update product.cost; parent stock_quantity orphan for variant products (marketplace in_stock + low-stock lists read parent only); variant-scoped barcodes read-only; primary-vs-alternate/pack barcode namespace overlap; price_tiers not monotonic-validated; khata overpay silent; no per-dish prep-time field; pharmacy form missing expiry/batch/dosage inputs; Rx image capture absent; no scheduled near-expiry alert; combo cancel/return reads LIVE composition (not sale-time snapshot); split-by-item can't split one line's qty; services not marketplace-listed by default; owner reservation routes lack feature gate; ProductController hand-rolls envelope + inline validate in Gallery/Reservation controllers; null business_type tenant = all features off (factory state).

**2026-07-25 THIRD audit (verify-workflow lens agents, ultracode)** — refuter panel died on session limit so findings were self-verified by reading + regression tests. 14 findings, 13 FIXED same session (backend 557 green): P0 phantom tax refund (trusted `tax=>0` sales still snapshotted product catalog rate onto lines → returns refunded uncollected tax; fix = blend settled tax across taxable base in CreateSaleAction, stamp every line); P0 order-hold idempotency key was per-PRODUCT not per-LINE (duplicate lines → only first held → phantom stock on cancel/complete; fix = key by orderItem->id, matches releaseStock); P1 CloseCashSessionAction ignored returns (refunded sale's cash vanished + cash refunds never subtracted; fix = include partially/fully-refunded sales + subtract cash SaleReturns' refund_total−refund_credit); P1 credit tender uncapped (fat-fingered khata amount → cash change payout; fix = 422 CREDIT_EXCEEDS_DUE when amountPaid>total with credit); P1 duplicate sale_item_id rows refunded twice/restocked once (fix = `distinct`); P1 CSV re-import silently flipped item_type (normalize defaults physical_product; fix = strip item_type/type/track_inventory on update path); P2 sequential-return rounding over-refund (fix = cumulative allocation, final return absorbs remainder); P2 chargeCredit no row lock (fix = lockForUpdate before charge, mirrors reversal paths); P2 OrderService `catch(QueryException)` unimported = dead idempotency recovery (fix = add import); P2 deal duplicate component (fix = `distinct`); P2 PlanSeeder missing derived `products` key; P2 sale_items.tax_rate no decimal:2 cast; P2 AdminPlansPage description couldn't be cleared (undefined dropped; fix = send null, widen PlanInput). Added 5 per-domain edge-case test files (Food/MartRetail/Pharmacy/Services/PlansLimits, 38 tests). **Combo restore reading LIVE recipe (was P2 backlog): FIXED for CANCEL + order-RELEASE via stock_movements replay (reverse recorded 'out' movements, immune to mid-life recipe edits) — the partial-RETURN combo-after-recipe-edit path STILL reads live recipe (needs qty proration; only remaining corner).**

**2026-07-29 — that last corner is now FIXED (backend 621 tests green +1 RecipeBomTest regression).** Approach: a per-line BOM SNAPSHOT rather than movement-replay (returns are partial + per-line; movements aren't line-scoped so replay can't attribute a shared component to one returned line). New `sale_items.components` JSON = exploded per-unit BOM captured in CreateSaleAction via `bomSnapshot($product)` (tracked physical combo components / recipe ingredients only; null for plain products). ProcessSaleReturnAction restocks from the snapshot (`quantity_per_unit × returned_qty`, natural proration) in preference to the live recipe; legacy pre-snapshot lines fall back to the live `isCombo()`/`hasRecipe()` read. Proven: sell burger [bun×2,patty×1], EDIT recipe to [bun×5,cheese], return 1 → restocks 2 bun + 1 patty (as sold), cheese untouched. **All P0 stock-loss corners now closed — offline POS is no longer gated by this bug** (see [[shopos-offline-plan]]).

**2026-07-31 — P2 POS edge-case cluster: 5 of 7 FIXED (backend 719 green, +10 tests; panel tsc+build green).** Verified each against live code first (Explore pass). (2) **return_number race** — new generic `document_counters` table (tenant_id,type PK) + `App\Support\DocumentCounter::next()/formatted()` (locked-row increment, mirrors invoice_counters); ProcessSaleReturnAction now mints RET- from it, not `count()+1`. Other `count()+1` sites (PO/order/ticket/KOT/transfer) left for a later sweep — helper makes them one-liners. (6) **price_tiers non-monotonic** — `StoreProductRequest::validatePriceTiers()` (reused by Update) rejects duplicate min_qty + a higher-qty tier priced dearer; `Product::priceForQty()` now charges the CHEAPEST qualifying tier (was: deepest min_qty → could overcharge). (7) **khata overpay silent** — `CustomerController::recordPayment` throws `KHATA_OVERPAYMENT` (422) when amount > outstanding unless `allow_advance:true` (new optional bool on RecordCustomerPaymentRequest) → deliberate advance; model `recordCreditPayment` unchanged (still used capped by return/cancel). (4) **variant parent stock orphan** — `Product::effectiveStock()` (sum variants else own) + `isLowStock()` uses it; ProductController low-stock filter (index + CSV export) rewritten to whereDoesntHave-vs-whereHas variant subquery; MarketplaceController `in_stock` uses effectiveStock. (5) **barcode namespace overlap** — SyncProductBarcodesAction now also rejects an alternate barcode matching any variant SKU or pack (ProductUnit) barcode (POS scan lookup resolves those, so a product-level alt would shadow them). **#1 exchange-tender was ALREADY CORRECT** (refund_method defaults to `sale->payment_method`, not cash — only split-tender collapses to one method, minor). **#3 split-by-item now DONE too** (backend 721 green, +2 tests; panel tsc+build green): SettleTicketRequest accepts `splits:[{id,quantity}]` (takes precedence over item_ids); SettleTicketAction::resolveSplits + splitRow carve a paid row off a line (prorate line_total/line_discount by qty ratio, remainder stays open, sums back to the paisa; caps at unpaid qty → SPLIT_QTY_EXCEEDED). Panel TabPage settle modal replaced item checkboxes with per-line −/＋ quantity steppers (always sends `splits`; full-qty split = whole line server-side). So ALL 7 POS edge-cases are resolved. Also this session: **POS scan/add success+error beeps** (Web Audio, no assets — `src/modules/pos/posSound.ts`, rising chirp / low buzz, localStorage mute toggle in PosPage scan bar). **COMMITTED + PUSHED 2026-07-31: backend `backend` 08cccaa, panel `admin-panel` 8a32e2f** (covers all 7 POS edge-cases + split-by-qty + POS sounds + pharmacy form depth). Reminder: push triggers the still-broken auto-deploy (see [[shopos-deployment]]) — live site won't update until deployment is fixed.

**2026-08-06 — M1–M4 (the MEDIUM tier from the business-type audit) ALL FIXED. Backend 1184 green / 4964 assertions (+10 tests); panel 73 green (+11).** Decisions worth remembering because they are not obvious from the code alone:
- **M2 — the nav has THREE axes now: module, trade, and PERSON.** `shopNav(features, type, mode, multiBranch, can)` — every item/sub-item carries a `can` naming the permission the SERVER asks for on that screen's own action, and a dropdown emptied by permission is dropped with its children. Deliberate calls: Day & banking = `sales.manage` (a cashier is entitled to their own drawer's record; the day CLOSE stays manager-only server-side); Reviews = `settings.manage` (replying is the only action there); Subscription = NO gate (the server asks none — what the shop pays is not secret from staff). Complemented by a new `RequirePermission` route guard across the tenant routes, because a hidden link is a courtesy, not a lock.
- **M3 — `BusinessTypes::primary()` is the ONE legacy→current translation**, exposed to the panel as `tenant.business_type_primary` (raw `business_type` stays for display/admin). Panel reads it via `usePrimaryBusinessType()` in sidebar + POS + product form + dashboard capabilities. **`workshop` was remapped services→automotive** — its module set (products+services+inventory, sold at the shop) IS automotive's, and a mechanic wants the plate register and part-shaped units over a portfolio.
- **M4 — `EnsureFeature` is now variadic and reads as ANY-of** (`feature:products,services`); the catalog carries that gate on both sides (panel `RequireFeature feature={["products","services"]}`). Books-only now gets a straight MODULE_DISABLED instead of being stopped later by the item-type registry.
- **M1 — a recipe is a STOCK feature.** Non-empty recipe without the inventory module → 422 `RECIPE_NEEDS_INVENTORY` (clearing one stays open); naming a product as an ingredient switches `track_inventory` ON for it and returns a warning; untrackable (service) ingredients are named as not-deducted. Panel hides the section without the module AND stops sending `recipe_items` at all then, so editing a dish's price can't silently wipe its recipe.
- **DASHBOARD follow-up DONE same day (panel 89 green).** User declined to pick between hide-figure/hide-link and said "handle accordingly, market level", so: **a tile is drawn only when the person can open the screen behind it** — the rule Loyverse/Square/Vend/Odoo all use, and the same rule as the sidebar. Refactored to ONE map, `src/common/routing/screenPermissions.ts` (path → permission), read by sidebar + dashboard + route guards; the sidebar's per-item `can` tags were REMOVED in favour of it. Exactly three screens deliberately unmapped (`/tenant` = home + redirect target, `/tenant/setup`, `/tenant/subscription`) and a test asserts that list is exact — a screen added without a rule silently falls through as open to every cashier. Net effect: cashier keeps day/banking/parked-tickets/subscription warnings, loses total khata + supplier dues + stock rows. Shared route list at `src/test/routes.ts`.
- **Backend commit f18ea13 (`backend`), panel 475e56a then 170632d (`admin-panel`) — pushed 2026-08-06.**

Business-Type-Engine doc gaps (feature work, not bugs):
- **SHIPPED 2026-07-25 (backend 7d163ef 581 green +6 BusinessTypeEngineTest; panel 086fb59):** per-type **units** (`BusinessTypes::UNITS` + `unitsFor()`, legacy→primary fallback) and **variant attribute** suggestions (`VARIANT_ATTRIBUTES` + `variantAttributesFor()`); **online-required fields** (`ONLINE_REQUIRED_FIELDS = [image, description]`) exposed as `online_required` per marketplace item type. BusinessTypeController surfaces `units`/`variant_attributes`/`online_required`. ProductFormPage: base-unit suggestion chips, variant-attribute hint line, and online-required enforcement (description required inline + blocks save, photo prompted) when a marketplace-visible item is on an online shop.
- **STILL OPEN:** per-type REQUIRED-field matrix enforced server-side (medicine opening-stock expiry IS already requiredIf; broader matrix not); batch recall flow; appointments add-on (services depth); the online image-required is a UI prompt only (backend emits no hard 422 — images upload post-create).
- **2026-07-31 PHARMACY FORM DEPTH DONE** (backend 723 green, +2 tests; panel tsc+build green): migration `2026_07_31_000007` adds `products.strength` + `products.dosage_form` (nullable, descriptive). Store+UpdateProductRequest validate them; CreateProductAction persists them; MarketplaceController publicProduct exposes strength/dosage_form. NEW `opening_batch_number` on StoreProductRequest (create-only, not a column) → CreateProductAction uses it for the product- AND variant-level opening lots (was hardcoded 'OPENING'; still defaults to 'OPENING' when blank). Panel ProductFormPage "Medicine details" section gained a Strength Input + Dosage-form Select (Tablet/Capsule/Syrup/…); opening-stock area gained an Opening batch number Input beside the expiry. generic_name + Rx toggle + opening expiry already existed. See [[shopos-pharmacy-edges]].

Remaining pre-launch web items (see [[shopos-plans-and-flow]]): ② super-admin config UIs (Modules/Add-ons/Business-Types/Marketplace/Riders/Notifications/Settings pages — backend partly exists), ③ merchant polish (global search, bulk export, Basic/Advanced mode), ④ end-to-end onboarding smoke-check. Then multi-branch (Step ④) + retail/services depth as v1.1.
